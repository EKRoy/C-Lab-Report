#include <bits/stdc++.h>
using namespace std;
int main()
{
start:

    int si;
    cout << "Press 1 for Yes Or 0 for NO" << endl;
    cin >> si;
    if (si == 1)
        goto start;
    return 0;
}